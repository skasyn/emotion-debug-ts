# Using the example

```sh
# install yarn 2
yarn set version berry
# install dep
yarn install

# launch the app
yarn workspace @app/online-store dev

# see the error
yarn workspace @app/online-store build
```

